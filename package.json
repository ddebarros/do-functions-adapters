{
  "name": "do-function-adapters",
  "version": "1.0.0",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "build": "run-s build:clean build:ts",
    "build:ts": "tsc",
    "build:watch": "tsc --watch",
    "build:clean": "rm -rf build",
    "prepare": "yarn build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.7.18",
    "npm-run-all": "^4.1.5",
    "typescript": "^4.6.4"
  },
  "dependencies": {
    "@adobe/openwhisk-action-utils": "^4.4.8"
  }
}
